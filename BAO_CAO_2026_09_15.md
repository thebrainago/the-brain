# 15/09/2026 — Làn nhanh đã qua MT5 tester thật

## Kết quả (đây là phần quan trọng)

Ba hệ ở làn nhanh cuối cùng đã chạy được trên MT5 tester thật, tài khoản XM
`XMGlobal-MT5 10`. Đo trên **nửa holdout** — tức nửa dữ liệu mà hệ chưa từng
được nhìn khi chọn tham số.

Cửa sổ 19/03/2021 → 29/07/2026 (5,36 năm) · lot 20 · vốn 10.000 USD.

Cột cuối là **%/năm ở cùng mức sụt giảm 20%**. Phải quy về cùng rủi ro mới so
được: đọc thẳng cột "lãi" thì một hệ lãi nhiều chỉ vì nó đánh liều hơn.

### AUDCAD H4 — 4/5 hơn mốc mua-giữ
| hệ | lệnh | lãi USD | PF | sụt giảm | %/năm ở DD20 |
|---|---:|---:|---:|---:|---:|
| rsi_dao_chieu | 60 | +2.124 | 3,05 | 5,1% | **+11,92** |
| ou_quay_ve n50 mua | 56 | +1.765 | 2,75 | 6,5% | +8,45 |
| ou_quay_ve n200 mua | 30 | +1.872 | 4,71 | 7,2% | +8,10 |
| ou_quay_ve n50 bán | 46 | +896 | 1,75 | 5,8% | +5,15 |
| *mốc mua-giữ* | 17 | +528 | 1,18 | 16,9% | *+1,14* |
| ou_quay_ve n200 bán | 27 | −164 | 0,91 | 10,3% | −0,60 |

### EURGBP H4 — 6/6 hơn mốc (mốc rất tệ)
| hệ | lệnh | lãi USD | PF | sụt giảm | %/năm ở DD20 |
|---|---:|---:|---:|---:|---:|
| ou_quay_ve n200 mua | 26 | +1.832 | 3,73 | 6,3% | **+8,95** |
| ou_quay_ve n50 bán | 58 | +2.695 | 2,39 | 9,5% | +8,76 |
| ou_quay_ve n200 bán | 26 | +2.510 | 4,44 | 13,9% | +5,90 |
| rsi_dao_chieu | 54 | +43 | 1,02 | 9,5% | +0,17 |
| ou_quay_ve n50 mua | 53 | +45 | 1,02 | 10,7% | +0,16 |
| mat_can_bang_lenh_dong_cua | 53 | −237 | 0,78 | 5,6% | **−1,65** |
| *mốc mua-giữ* | 17 | −3.276 | 0,31 | 37,8% | *−3,49* |

**Hai điều rút ra:**

1. **Tester thật không những khớp mà còn hơn bản Python.** Bản Python báo
   `rsi_dao_chieu` 8,75%/năm ở nửa sau; MT5 với spread và phí qua đêm thật cho
   11,92%. Đây là lần đối chiếu Python↔MT5 đầu tiên không lệch xấu.
2. **Hệ thứ ba âm.** `mat_can_bang_lenh_dong_cua` được bản Python xếp qua cổng
   (+3,61%/năm) nhưng trên tester thật là **−1,65%/năm**. Bỏ nó khỏi làn nhanh.

**Toàn kỳ 2013–2026 thì mỏng hơn nhiều** (AUDCAD tốt nhất +2,56%/năm ở DD20).
Edge nằm ở nửa sau — cùng hình dạng với các họ đã đo trước đây.

## Lỗi đã sửa hôm nay

### 1. `than_nen` mất dấu khi dịch sang MQL5 — 56/3189 cơ chế dính
`nhan/dich_mq5` dịch "thân nến" thành `MathAbs(close−open)`, còn bản Python là
`close−open` **có dấu**. Mọi cơ chế hỏi "nến giảm" (`than_nen < 0`) thành điều
kiện không bao giờ đúng → EA chạy đủ 5,4 năm, ghi đủ báo cáo, ra **0 lệnh**,
không một dòng lỗi. Bản Python cùng cơ chế cùng cửa sổ bắn 98 tín hiệu.

Cách phân biệt với hỏng môi trường: hỏng môi trường làm **mọi** cơ chế 0 lệnh
kể cả mốc mua-giữ; lỗi dịch chỉ làm **một** cơ chế 0 lệnh. Thấy đúng một dòng
0 lệnh thì đi đọc mã MQL5 nó sinh ra, đừng đọc log.

Đã sửa + hai test chặn trong `test_spec_hong_khong_im_lang.py`.

### 2. Bảng tester không so được ở cùng rủi ro
Thêm cột `%/nam@DD20` và dòng "mốc mua-giữ ở cùng rủi ro: … → n/m cơ chế hơn
mốc". Trước đó phải tự nhẩm, và tôi đã một lần so cột `lãi` với mốc có sụt
giảm gấp ba.

### 3. Tester ngoại suy đòn bẩy 285 lần từ một chữ số
MT5 in sụt giảm với hai chữ số thập phân. Lot 0,10 trên vốn 10.000 cho
`DD = 0,03%` — một chữ số ý nghĩa, sai số ±17% truyền thẳng vào kết quả. Thêm
`--lot` để đo ở cỡ lệnh thật, và cảnh báo khi DD < 0,20%.

### 4. `--loc` chỉ nhận một chuỗi
Ba hệ làn nhanh không vào chung một mẻ được (hai hệ tên có `_dsl_`, hệ thứ ba
họ `phien`). Mà TESTER=1 là ràng buộc vật lý — mỗi mẻ thừa là một lượt boot
terminal độc chiếm máy. Nay `--loc a,b,c` là HỢP của các bộ lọc.

## Việc tiếp
- Hai hệ AUDCAD sống → **demo**, chưa ra tiền thật.
- Chạy lại 56 cơ chế dùng `than_nen` — trước nay chúng ra 0 lệnh oan.
- `mat_can_bang_lenh_dong_cua`: bỏ khỏi làn nhanh, hoặc tìm vì sao Python và
  MT5 lệch dấu nhau.

---

# 15/09/2026 — PHẦN 2: ba cổng nằm ngoài đường chạy

Phiên này bắt đầu từ mục 2 của "Việc tiếp" ở trên (*chạy lại 56 cơ chế dùng
`than_nen`*). Việc đó mất 20 giây và ra kết quả âm. Ba thứ tìm được trên đường
đi thì đáng hơn nhiều, và cả ba cùng một hình dạng: **bộ phận CÓ tồn tại, luật
CÓ viết ra, nhưng nó không nằm trên đường chạy — và hệ vẫn báo một con số bình
thường.**

## 0. Kết quả bằng số

| | trước phiên | sau phiên |
|---|---:|---:|
| cơ chế ra được MT5 tester | 2.654 / 3.216 | **2.991 / 3.217** |
| cơ chế không dịch được | 563 | **226** |
| cơ chế chết trên mọi cặp FX vì lỗi khai | không ai biết | **75 đã đo, 27 cứu được** |
| lượt tester chết vì sai tên mã | không chẩn đoán được | báo TRƯỚC khi boot |

Tiền: **chưa có gì mới**. Chi tiết ở mục 4.

## 1. Sai tên mã đốt một lượt boot terminal, và hệ không nói gì

`--ma AUDCAD` chạy hết một lượt rồi trả về `khong thay bang ket qua` trơ trọi.
Log MT5 nói thẳng một dòng: `Tester symbol AUDCAD not exist`.

Tài khoản đang đăng nhập là `XMGlobal-MT5 10`, chỉ có mã `...micro`. `AUDCAD`
trần chỉ tồn tại dưới server **cũ** `XMGlobal-MT5 17` (tài khoản hỏng từ 14/09).

Ba chỗ hỏng cộng lại, **không chỗ nào báo lỗi**:

1. Cổng kiểm tên mã **đã có từ 13/09** (`chay_bench_quan_tri.goi_y_ma` — chính
   nó tìm ra `XAUUSD → GOLD`) nhưng chỉ được gọi trong chính file đó. Đường
   chạy chính không bao giờ thấy nó.
2. Cổng đó gộp **tất cả** `bases/`, nên kho lịch sử của một tài khoản đã chết
   vẫn trả lời "có".
3. `DAU_HIEU_HONG` có `unknown symbol`, còn MT5 in `not exist` và
   `cannot select symbol`. Bảng dấu hiệu **không khớp một ký tự nào** với cái
   máy thật sự in ra.

Sửa: `nhan/ten_ma.py` — một nguồn sự thật, chấm theo server trong
`config/common.ini`, `--lam-moi` đọc **1.630 mã thật** từ terminal. `b ten-ma`.

Hệ quả phát hiện thêm: trên tài khoản này **không có `GOLD`**, chỉ có
`GOLDmicro` — trong khi `BI_DANH` vẫn ánh xạ `XAUUSD → GOLD` và thư mục lịch sử
tên `GOLD` vẫn nằm đó. Mọi lượt bench trên vàng trước đây sẽ ra 0 lệnh.

## 2. Ngưỡng tính bằng ĐƠN VỊ GIÁ — 13 cơ chế ra 0 lệnh và cả hai bản đều đúng

Chạy lại 61 cơ chế `than_nen` với tên mã đúng: **13 cơ chế ra đúng 0 lệnh**.
Bài học hôm trước nói "một dòng 0 lệnh thì đi đọc mã MQL5", nên tôi đọc — và bộ
dịch không có lỗi: **bản Python cũng bắn 0 tín hiệu**.

Lỗi nằm trong chính lời khai:

    ty_le_than_nen:   than_nen > 0.5

`than_nen` là `close − open`, **đơn vị giá**. `0.5` là số trần. Đo trên sáu
chuỗi **cùng khung H1**:

| chuỗi | giá TB | tỉ lệ kích hoạt |
|---|---:|---:|
| EURUSD | 1,17 | 0,000 |
| AUDCAD | 0,95 | 0,000 |
| GBPUSD | 1,51 | 0,000 |
| USDJPY | 110,88 | 0,005 |
| XAUUSD | 1.313,92 | 0,364 |
| US500Cash | 3.896,84 | 0,532 |

Đi từ 0,000 đến 0,532 **chỉ theo mức giá**, không theo hành vi giá. Cái tên
(`tỷ lệ` thân nến) nói tác giả định viết `than_nen / bien_do`, một số 0..1.

**Vì sao nó sống sót lâu:** cửa vào kho đo tỉ lệ kích hoạt trên hai chuỗi rồi
`break` ngay ở chuỗi đầu tiên đạt. Cơ chế vào kho mang theo con số của XAUUSD
(0,5202) — một con số hoàn toàn khoẻ mạnh — rồi ra 0 lệnh trên mọi cặp FX mãi
mãi, mỗi lần chỉ hiện ra là một dòng 0 lẫn giữa các dòng thật.

Quét cả kho (`b thang-gia`): **75 PHỤ_THUỘC_THANG_GIÁ · 108 CHẾT_MỌI_NƠI ·
210 THƯA_THỚT** trên 3.233.

**Bản đồ đầu tiên sai và phải bỏ đi.** Nó trộn khung (AUDCAD H4, US500Cash D1)
nên xếp `buy_liquidity_sweep_ob_reversal` là "chết ở giá cao", trong khi cái
khác nhau thật sự là KHUNG. Một bộ đo muốn quy kết cho thang giá thì mọi thứ
khác phải giữ nguyên.

Cổng nay nói TRƯỚC khi boot terminal cơ chế nào sẽ ra 0 lệnh vì lỗi khai. Đo
lại trên AUDCADmicro: nó gọi **đúng 13 dòng** `CHUA_DO`.

## 3. `tuyen_tinh` — ngữ pháp nói được mà bộ dịch không nói được

Chuẩn hoá hằng số đơn vị giá thành `k × ATR(14)` (giữ độ hiếm của sự kiện, đúng
nguyên tắc `ngoai_sinh.chuyen`). `than_nen > 0.5` thành `> 0,0585 × ATR(14)`:
kích hoạt **0,45–0,48 đều nhau ở cả sáu bậc giá**.

Nhưng 27 bản chuẩn hoá ra tester thì **"dịch được 1, bỏ 27"**: bộ dịch từ chối
thẳng mọi nút có khoá `toan_hang`. Ngữ pháp có `tuyen_tinh` từ 01/09 —
Bollinger/Keltner đều viết bằng nó — mà MT5 chưa bao giờ nhìn thấy.

Đo tiếp thì ra con số thật của nút thắt: **563/3.216 cơ chế hợp lệ không ra nổi
tester**. Đã bù: `tuyen_tinh` · `adx` (3 đường, cần `ChiB` đọc buffer khác 0) ·
`cci` · `donchian` (5 biến thể) · `keltner` (5 biến thể) · `stochastic` ·
`dong_luong` · `macd` (chỉ đường chính) · bốn toán tử GỘP danh sách.

**Ba chỗ TỪ CHỐI thay vì dịch gần đúng**, vì mỗi cái đều có một hàm MT5 "gần
giống": `macd` đường tín hiệu (quy ước EMA của chính đường MACD) · `cci` trên
`ohlc4` (`PRICE_WEIGHTED` của MT5 là `(h+l+2c)/4`, **không** phải `(o+h+l+c)/4`)
· `keltner` của biểu thức lồng. Dịch gần đúng sinh ra một EA khác với lời khai —
vẫn chạy, vẫn ra số.

`donchian` dịch **một bar** (`s+1`) y như bản Python. Thiếu chỗ đó thì
`gia >= kenh_tren` LUÔN đúng ở mọi đỉnh mới — nhìn trước kín nhất trong họ này,
và nó hiện ra thành một hệ "bắt đỉnh" rất đẹp.

## 4. Tiền: ba lượt đo, không lượt nào ra hàng mới

| lượt | kết quả |
|---|---|
| 61 cơ chế `than_nen`, AUDCAD H4 holdout | **0/59** hơn mốc |
| 61 cơ chế `than_nen`, EURGBP H4 holdout | 3/59, mà mốc là −3,07%/năm |
| 27 bản chuẩn hoá `k × ATR`, AUDCAD | **1/27**, +1,24 so với mốc +1,13 |
| 289 cơ chế donchian/keltner/cci/adx, AUDCAD | 14/289 hơn mốc, đỉnh **+7,86%/năm** @DD20 (Sharpe 1,58 · PF 1,73 · 116 lệnh) |
| cùng 289 cơ chế, EURGBP | 19/289 — **nhưng quán quân AUDCAD không có trong đó** |

Đỉnh +7,86% là **cực đại trong 289 phép thử** và không lặp lại trên tài sản thứ
hai. Chỉ hai cái dương ở **cả hai**: `ns_keltner20_<_q2_giu10` (+1,71 / +2,92)
và `ns_cci20_<_q2_giu5` (+2,00 / +0,90) — cùng hướng với họ `ou_quay_ve` đã
sống, tức mua khi giá rơi xuống phân vị thấp trên cặp chéo FX. Xác nhận yếu cho
một họ đã biết, không phải phát hiện mới.

**75 cơ chế chết trên FX không phải kho báu bị giấu.** Khi đo được thì chúng đo
ra là vô dụng — phần lớn âm nặng vì kích hoạt 45–95% số bar, tức là phí.

## 5. Việc tiếp

1. **226 cơ chế còn lại chưa ra được tester**: `mau_nen` 135 · `supertrend` 40 ·
   `heiken` 40. `mau_nen` là món lớn nhất còn lại và nó chỉ là boolean từ OHLC
   vài bar — dịch được.
2. **Hai hệ AUDCAD đã qua tester (13/09) vẫn chưa ra demo.** Đường còn thiếu là
   MT5 → demo → tiền thật, không phải thêm một hướng nghiên cứu.
3. `b thang-gia` mới chạy trên kho hiện tại; nên gắn nó vào cửa vào kho để cơ
   chế mới không lọt thêm.
4. `BI_DANH` ánh xạ `XAUUSD → GOLD` nay sai trên tài khoản đang dùng.
