# BAO CAO 13/09/2026 — SUA HE, KHONG PHAI SAN CO CHE

Chu du an dat lai muc tieu giua phien: *"Muc tieu van chua phai la ra nhieu co
che hay kiem tien ma cai he thong phai tot da. Cac module va tru la de lam co
so va phuong phap cho boc tach va test sau nay. Nen trong qua trinh lam tap
trung quan sat de xem van de phat sinh roi sua de nang cap va hoan thanh he
thong."*

Bao cao nay xep theo do nghiem trong cua LO HONG, khong theo thu tu lam.

---

## 1. MAT DU LIEU: kho co che bi ghi de 2.975 -> 21, HAI LAN

Nghiem trong nhat trong ngay. Da khoi phuc du (**3.033 co che**, khong mat gi).

### Chuoi nhan qua day du

```
o C tut ve 233 MB
   -> pytest 6 nhan chet voi `paging file too small`
   -> mot lan ghi file khong tron ven
   -> `doc_kho()` doc loi, `except Exception: return []`
   -> `them_co_che`: ds = [] -> append -> `luu_kho([1 spec])`
   -> `luu_kho`: `cu = len(doc_kho())` cung = 0
                 chot viet la `if not ep and cu and moi < cu*0.8`
                 -> `cu == 0` nen menh de TAT, chot khong chay
   -> ghi de 2.975 co che bang 1
```

Va mot duong thu hai, doc lap: `test_chan_hang_so.py` doc kho **THAT**, them
mot spec hong, roi tra lai trong `finally`. Chay `-n 6` thi hai nhan doc-sua-ghi
cung mot file; va khi `finally` khong chay, spec rac o lai trong kho san xuat
(no da lam do mot bai kiem KHAC trong chinh file do).

### Nam chot da them

| chot | o dau | chan cai gi |
|---|---|---|
| `doc_kho` phan biet BA truong hop | `ngu_phap.py` | khong co file -> `[]`; doc khong ra -> nem `KhoDocHong`, **khong bao gio** `[]` |
| chot hao hut theo **MOC CAO NHAT** | `ngu_phap.py` | bao mon dan (2.741->130 chi can 15 buoc moi buoc mat 19%, chot cu khong bat duoc) |
| ban lui `.json.lui` truoc moi lan ghi | `ngu_phap.py` | chinh no da cuu du lieu lan thu hai |
| nhat ky ghi kem VET GOI | `nhat_ky/kho_co_che_ghi.log` | suy luan tu ma nguon het duong, can vet goi that |
| bai test khong duoc sua du lieu san xuat | `conftest.py` + `test_chan_hang_so.py` | canh theo TUNG BAI khi mot nhan, theo CA PHIEN duoi xdist |

Test: `test_kho_khong_teo.py`, 8 bai — moi bai la **mot duong di da tung lam
mat du lieu that**.

---

## 2. DIA DAY: nguyen nhan goc, va tai sao canh bao bang chu khong du

O C con **233 MB** luc 10:20. Da lay lai len **18 GB** (nay on dinh ~8 GB sau
khi Windows tu no `pagefile.sys` tu 4,8 -> 15,2 GB — chinh la thu sua loi
`paging file too small`).

Don duoc, khong dung du lieu nao cua chu du an:

| don gi | lay lai |
|---|---|
| `Tester/logs` + `Tester/cache` cua MT5 (tu sinh lai) | ~1,4 GB |
| bao cao `.htm` cu >2 MB (bang `.xml` van giu) | ~0,3 GB |
| `liveupdate` + `temp` cua MT5 thu hai | ~0,5 GB |
| cache Chrome/CocCoc/Opera (**khong** dung profile — Seeker can tai khoan da dang nhap) | ~13 GB |
| `npm-cache`, `pip/cache`, Temp cu hon 1 ngay | ~1,2 GB |

**Diem quan trong khong phai con so GB.** Ban giao hom truoc da ghi dung cai
bay nay bang chu: *"Dia day khong hien ra nhu loi dia. No hien ra nhu 'boc 684
file -> 0 co che'. Kiem `b don-dia` TRUOC khi tin bat ky ket qua rong nao."*
Canh bao do duoc viet ra, doc ky, **va van sap lai**. Nen no phai thanh mot
CONG: `nhan/dia.py`, chan o `luu_kho` / `boc_llm.boc` / `go_html.go_kho`.

**Con can nguoi quyet**: `Pictures` 14,6 GB · `AppData\Roaming\Apple Computer`
(sao luu iPhone) 5,7 GB · hai thu muc MT5 cu `1A84…` va `5378…` (~1,2 GB, sua
lan cuoi 01/09, co ve la san khac).

---

## 3. PHEU SEEKER DUT MOT KHAU — "97% kho chua boc" khong phai bai toan thong luong

Do tren dung hang ton:

```
5.486 ban chua boc, cho qua bo loc dau vao `boc_llm.ung_vien`:
    3.948  bi `_la_html_tho` chan   (72%)
    1.530  cham 0 dau hieu luat
        8  QUA
```

`boc_llm.boc(300)` tim duoc **8 ung vien** tren ca kho. Khong so luong nao,
khong model nao cuu duoc con so do. Trong 3.948 ban bi chan: **2.727 trang repo
github** (~289.000 ky tu boilerplate moi trang) + **859 trang mql5.com/code**.

Bo loc **khong sai** — no chan dung rac. Cai sai la **khong co ai dung sau no**.

### Da lam: `nhan/go_html.py` (khau con thieu)

| | truoc | sau |
|---|---|---|
| ung vien qua pheu | **8** | **785** |
| kho HTML tho | 3.948 | 16 |
| dung luong | 210 MB | 5,4 MB (giam 38,7 lan) |

4,5 phut voi 14 **tien trinh** (khong phai luong — `bs4` giu GIL nen 8 luong =
1 luong). Noi vao `day_chuyen.boc` giua 2a va 2b, va `b go-html`. 8 test.

### Va khau di lay PAYLOAD that — lo hong doc lap, da sua

Trang `mql5.com/en/code/43355` dai 62.924 ky tu chua **0 lan** `OnTick`/
`OrderSend`/`CTrade`: ma nam **sau nut tai**. Hoa ra khau tai **da ton tai**
(`ma_nguon.tai_ma_nguon`, noi vao duong chinh tu 03/09) nhung:

- cot `cach='mql5_download'` chi co **25 dong, toan bo trong dung ngay 03/09**,
  trong khi ba luot quet 08–12/09 tao them **813 dong HTML trang**;
- nguyen nhan: `_lay()` cua no khong co tang vuot DNS nhu bo doc chung, nen khi
  DNS bi dau doc no **that bai im lang** va roi ve bo doc HTML — bo nay lay
  duoc **trang landing** roi ghi vao `noi_dung` nhu mot lan doc THANH CONG.

Da them tang vuot DNS + `co_dau_hieu_ma_that()` + `thu_hoi_sai_loai()` (bo cuoc
sau 3 lan, khong khoa vinh vien). Do that tren 40 URL co san: **31 sua duoc**
(loat hai 14/15 = 93%). Va `thu_hoi_khong_doc_duoc()` — dinh nghia tu 01/09 —
**chua tung duoc goi o dau** (dead code, 233 tai lieu bi khoa oan); da noi day.

### Phan ton con lai la THAT SU ngheo tin hieu

3.734 ban con lai: lay mau 1.200, **1.200/1.200 cham 0 diem** — header giay
phep, tom tat hoc thuat, boilerplate framework. Day la mot ket luan am tinh
dung nghia, khong phai mot khau do hong.

---

## 4. QUAN TRI LENH: ket luan "AM" hom qua la khau do hong

`reports/DE_QT_EA_NGOAI.json` (12/09) ghi 14/15 luat cho so lieu **giong het
nhau den tung so le** (1759 lenh, lai −108,76) roi tu ket luan `"AM"`. Hai
nguyen nhan:

1. Bang tham so sinh ra **toan so 0** — `dich_mq5_qtvt._atr0` chi nhan
   `{"atr": x}` ma **141/146** gia tri trong kho la `{"pip": x}`. Ham quy doi
   `sang_atr()` co san nhung **chua khau nao goi**.
2. Ke ca bang dung, EA ngoai tu dat TP/SL va tu thoat theo tin hieu — quan tri
   khong con **cho** de hanh dong.

Da sua ca hai duong:

**(a) Duong cu** — quy doi don vi bat buoc (nem loi thay vi roi ve 0), cong
`kiem_bang_luat()` tu choi bang toan 0, va `so_luat_doi_so_lenh == 0` nay luon
ra `CHUA_DO_DUOC` chu khong bao gio `AM`. Bang sau khi sua co gia tri that va
khac nhau. 19 + 46 test xanh.

**(b) Duong moi — BAN DO `b bench-qt`** (`ea_QuanTriBench.mq5`): ENGINE VAO CO
DINH, khong TP, chi mot luoi an toan; `InpHoQT=0` la MOC nam trong chinh EA do.
11 ho: moc · tp_co_dinh · trailing · dat_hue · hue_trailing · tia · hedge ·
stop_2_dau · tt_stop_doi · luoi_dca · thoi_gian. 275 pass mot lan boot (~90 s).

### Ket qua: 3 ma x 3 engine x H1 x 2016-2026, 9 luot dung duoc

XAUUSD bi cong loai dung 3/3 luot — **ma do khong ton tai tren XM**, vang o day
ten la `GOLD`. Da them khau kiem ten ma truoc khi chay.

```
ho            cao_nguyen  lai>moc  DD<moc   tren deu_dan
stop_2_dau       7/9        9/9      9/9        2/3      va DUONG tuyet doi 9/9
trailing         6/9        8/9      8/9        2/3      <- lat nguoc ket luan 12/09
hue_trailing     6/9        7/9      7/9        2/3
tia              6/9        7/9      7/9        2/3
dat_hue          5/9        9/9      9/9        1/3      quan tri THUAN, giu chieu
hedge            4/9        6/9      5/9        1/3
luoi_dca         4/9        5/9      3/9        2/3      lai to nhung DD toi 98%
tt_stop_doi      4/9        4/9      3/9        1/3
tp_co_dinh       3/9        5/9      5/9        1/3
thoi_gian        0/9        6/9      6/9        0/3      yeu nhat
```

**Doc con so nay cho dung:**

- Moc AM o ca 9 luot (engine vao co tinh ngo ngan). "Hon moc" khong dong nghia
  "ra tien". Nhung **`stop_2_dau` va `luoi_dca` DUONG tuyet doi**, va tren
  US100Cash/quay_ve `stop_2_dau` cho Sharpe 1,29 voi DD 6,08%.
- **`stop_2_dau` dat buy stop VA sell stop hai ben nen khong dung CHIEU cua
  tin hieu vao.** Vi vay viec no "qua duoc engine ngau nhien" mot phan la vi
  no vut bo chieu ngau nhien do. **No la mot co che VAO doi lot quan tri** —
  dung xep chung hang voi `dat_hue`.
- `dat_hue` (dat hue von) hon moc ca LAI lan SUT GIAM o **9/9** luot. Day moi
  la tac dung cua quan tri thuan: giu chieu, giu entry, chi doi cach thoat.
- `luoi_dca` lai to nhung sut giam 98% — luoi an toan `nhoi_max=5` chua du.

### Ba chot cua ban do, deu tu loi sap trong chinh ngay do

- `so_lenh == 0` / thieu moc / MOI ho y het moc -> `CHUA_DO_DUOC`.
- **Cap ho trung nhau tren CA LUOI** -> it nhat mot ho khong chay. `tia` ==
  `dat_hue` hai lan: lan dau vi lot goc 0,10 = dung lot toi thieu; lan hai vi
  `1,00 − 0,90 = 0,0999…` < vmin trong so hoc dau phay dong. Da chan ca hai.
- Bao ca `sharpe_trung_vi` + `ty_le_o_hon_moc` + nhan **cao_nguyen / lo_cho /
  cai_gai**, xep hang theo cao nguyen chu khong theo cuc dai cua 25 o.

---

## 5. TONG QUAN TAI SAN: 5 hon dao roi -> mot cua

Nam module da chay ra du lieu tot tu truoc (`tinh_cach`, `ho_so_symbol`,
`ho_so_mua_vu`, `ho_so_tuong_quan`, `ho_so_song`) nhung **khong module nao khac
trong lab doc chung**.

Da xay `nhan/ho_so_tai_san.py`: `ho_so()` · `tom_tat()` · `chon()` ·
`ghep_duoc()` · `tuong_quan(a, b)` · `thong_ke_tong_quan()` · `sinh_bang_md()`.
Moi con so kem **do tin** (co mau, p, do on dinh) va don vi; thieu du lieu thi
tra co thieu, khong bia mac dinh. Ra `reports/BANG_TAI_SAN.md` (159 dong). 25
test.

### Con so that (phan "khong hoi hot")

- **Mua vu**: 71 dong `dat=true`, nhung chi **31** co spread DO THAT, va chi
  **21 dong / 18 ma** vua `dat=true` vua **song qua chinh spread cua no**.
- **Tuong quan D1**: 306/340 cap co |r| ≥ 0,7 la ON DINH (ghep danh muc duoc);
  45/158 ma co doi tac AM on dinh de ghep chan nguoc; **128/158 trung rui ro**
  voi it nhat mot ma khac; 59/158 gan doc lap voi ca kho.
- **Song H4**: chi **27/159 ma** do duoc zigzag — 132 ma con lai la
  `CHUA_DO_DUOC` vi du lieu goc chi co D1, khong phai am tinh.
- **Do phu ca 5 nguon**: 157/159 ma.

### Va mot lo hong nhan da sua tan goc

Nhan "thu trong tuan" lech mot ngay tren **82/159 ma**, anh huong 41/50 phat
hien M3. Nguyen nhan that: bar D1 cua nguon SAN dong dau **dung 21:00 UTC suot
lich su** (nua dem gio may chu UTC+3, khong doi theo DST), nen ngay-lich tinh
theo UTC luon it hon ngay giao dich dung 1 ngay. Ban va `+1` cu tinh dung huong
nhung dua vao mot chi dau GIAN TIEP (%T6 thap/%CN cao) thay vi co che that.

Da viet lai doc thang gio UTC lam tin hieu chinh, giu thong ke lam xac nhan
cheo, va **hai tin hieu mau thuan thi ha thang ve `CHUA_DO_DUOC`** — khong con
"dat=true kem canh bao" nua. Phat hien phu: bai kiem cu
`test_ho_so_quantlab.py::NhanThuCuaFX_PhaiDuocSUA` dung fixture sai hinh dang
du lieu that va **van "xanh" mot cach vo nghia** vi `set(np.unique(None)) ==
{None}`. Da sua fixture.

---

## 6. CONG CU MOI DA NOI VAO DUONG CHAY

Luat L7 cua lab: *cong cu khong nam tren duong chay thi bang khong co.*

```
b go-html [N]     go trang HTML tho trong kho ra van ban (khau TRUOC `b boc`)
b bench-qt [MA]   ban do 11 ho quan tri; `--quet` = da tai san x da engine
python _tong_bench_qt.py    tong hop ban do quan tri qua cac luot
python -m nhan.dia          xem con bao nhieu GB truoc nguong chan ghi
```

`go_html` da noi vao `day_chuyen.boc`. `dia.du_cho` da noi vao `luu_kho`,
`boc_llm.boc`, `go_html.go_kho`.

---

## VIEC CHO PHIEN SAU

1. **Chay `b bench-qt --quet` tren nhieu ma hon.** 9 luot la it; cot
   `tren deu_dan` moi co 3 quan sat nen chua ket luan chac duoc ho nao that su
   qua phep thu phan chung.
2. **`dat_hue` la ung vien quan tri THUAN dang theo** (9/9 ca lai lan sut giam,
   giu chieu). Nen thu ghep no vao cac he DA PASS thay vi chi do tren ban do.
3. **`luoi_dca` can luoi an toan that** truoc khi doc bat ky con so nao cua no
   (DD 98%).
4. **Tang lot cua ban do len nhieu lan lot toi thieu** khi chay tren ma moi —
   ho `tia` chet lang le neu khong.
5. Nang `pagefile.sys` hoac giu ~15–20 GB trong o C. Ca hai su co mat du lieu
   hom nay deu bat nguon tu day.
6. Phan ton 3.734 ban cham 0 diem: dung ep qua LLM. Duong dung la khau tai
   payload vua sua (mql5 download + github raw).
