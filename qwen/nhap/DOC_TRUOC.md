# qwen/nhap/ — bản nháp qwen soạn, CHƯA ai duyệt

Việc trong `NHIEM_VU.json` có trường `soan_ma` sẽ đẻ ra file ở đây. Chúng
**không tự chạy**: một script backtest do LLM viết mà tự chạy rồi tự ghi vào báo
cáo là cách nhanh nhất để một con số sai đi vào kết luận mà không ai truy được.

Duyệt bằng tay, chép sang `lab/` với `__main__` guard, rồi thêm dòng `lenh` vào
bảng việc.
